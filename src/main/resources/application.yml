server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chandrakanthrck/microservices-config.git
          default-label: main
          clone-on-start: true
